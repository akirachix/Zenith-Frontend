/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['aquasense-e472a26d7581.herokuapp.com'],
    },
  }
  
  export default nextConfig